{
    "$id": "Entity.json",
    "type": "object",
    "properties": {
        "id" : {
            "$ref": "#/definitions/entityId"
        },
        "name" : {
            "$ref": "MultiLangString.json"
        },
        "webpages" : {
            "type": "array",
            "items": {
                "$ref": "WebPage.json"
            }
        },
        "posts" : {
            "type": "array",
            "items": {
                "$ref": "Post.json"
            }
        },
        "tags" : {
            "type": "array",
            "items": {
                "$ref": "Tag.json"
            },
            "minItems": 1,
            "uniqueItems": true
        },
        "alias" : {
            "type": "array",
            "items": {
                "$ref": "#/definitions/entityId"
            },
            "uniqueItems": true
        }
    },
    "required": [
        "id",
        "name",
        "webpages",
        "posts",
        "tags"
    ],
    "additionalProperties": false,
    "definitions": {
        "entityId": {
            "type": "string",
            "pattern": "^[A-Za-z0-9\\.\\-_]+$"
        }
    }
}